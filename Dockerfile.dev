FROM node:22-alpine
WORKDIR /usr/src/app

# Install ALL dependencies (including dev — needed for nest start --watch)
COPY package*.json ./
RUN npm install

# Copy source last so package.json changes don't invalidate the layer
COPY . .

CMD ["npm", "run", "start:dev"]
